# 멀티탭

질문 하나를 여러 AI에게 동시에 던지고, 답을 나란히 놓고 보는 앱.
브라우저 탭처럼 **AI 하나 = 탭 하나**. 한 창에 최대 7개까지 연다.

```bash
npm run dev
```

`http://localhost:3000` — API 키가 없어도 모의 응답으로 전부 동작한다.

## 되는 것

- **동시 질문** — 열려 있는 탭 전부가 같은 질문에 각자 답한다. 병렬 스트리밍이라 빠른 모델부터 먼저 찬다.
- **지목 질문** — 입력창의 대상 선택에서 탭 하나만 고르면 그 탭만 답한다.
- **하나의 대화** — 모든 탭이 같은 맥락을 본다. 이전 턴에서 다른 AI가 뭐라고 했는지까지 포함해서.
- **독립 답변** — 켜면 다른 탭의 답을 가린 채로 각자 답한다. 서로 눈치 보는 걸 막는 장치.
- **토론 붙이기** — 열린 탭끼리 서로의 답을 읽고 이름을 대며 반박한다. 라운드는 누적된다.
- **종합 보기** — 합의 / 갈림 / 결론 세 토막으로 정리한다.
- **자동 토론** — 켜두면 답 → 토론 1라운드 → 종합까지 알아서 이어간다.
- **탭 보기 / 비교 보기** — 하나만 깊게 읽거나, 전부 열로 나란히 놓고 비교하거나. 좁은 화면에서는 브라우저가 그렇듯 한 번에 한 탭만 보여준다.

## 화면 구조

브라우저 창을 그대로 가져왔다. 위에 회색 크롬과 탭 스트립, 아래에 흰 콘텐츠 표면.
활성 탭만 아래 표면과 이어 붙고, 응답 중인 탭 밑으로는 로딩 선이 지나간다.
색은 탭을 구분하는 데만 쓰고 본문은 전부 무채색이다.

```
멀티탭                          모의 응답  자동 토론  [탭|비교]  새 대화
╭─────────╮╭──────────╮╭──────────────╮
│● Opus 5 ││● GPT-4o  ││● Gemini Flash│  +
┴─────────┴┴──────────┴┴──────────────┴────────────────────────────
 질문
 주 4일제가 실제로 생산성에 도움이 될까
──────────────────┬──────────────────┬──────────────────
 ● Opus 5    2.5s │ ● GPT-4o    1.8s │ ● Gemini    0.4s
 ────────────────  ────────────────   ────────────────
 답변…             │ 답변…            │ 답변…
──────────────────┴──────────────────┴──────────────────
 토론 붙이기  종합 보기
 ┌──────────────────────────────────────────────────┐
 │ 무엇이든 물어보세요                                │
 │ 열린 탭 3개   독립 답변                    보내기 ⏎│
 └──────────────────────────────────────────────────┘
```

## API 키

`.env.example`을 `.env.local`로 복사하고 있는 키만 채우면 된다.
키가 있는 provider만 실제로 호출되고, 없는 쪽은 자동으로 모의 응답으로 넘어간다.
섞여 있어도 된다 — 실제 응답 옆에 모의 응답이 나란히 서고, 모의인 쪽에는 `모의` 표시가 붙는다.

| provider | 환경변수 |
| --- | --- |
| Anthropic | `ANTHROPIC_API_KEY` |
| OpenAI | `OPENAI_API_KEY` |
| Google | `GOOGLE_GENERATIVE_AI_API_KEY` |

모델 목록과 호출용 id는 [lib/models.ts](lib/models.ts) 한 곳에만 있다. 모델을 바꾸거나 추가하려면 거기만 고치면 된다.

## 구조

```
app/
  page.tsx           창 전체 — 상단 바 · 탭 · 내용 · 입력
  api/ask/route.ts   여러 모델 병렬 스트리밍 (NDJSON)
components/
  TabStrip.tsx       탭 스트립과 탭 추가 메뉴
  Thread.tsx         대화 — 비교 보기(열 분할) / 탭 보기
  Prose.tsx          답변 본문 (문단 · 목록 · 라벨)
  Composer.tsx       입력창과 대상 선택
lib/
  models.ts          모델 7종 정의
  chat.ts            타입과 프롬프트
  useTabs.ts         탭 · 대화 상태와 스트림 처리
  mock.ts            키 없이 돌리기 위한 모의 응답
```

## 알려진 한계

- 대화가 저장되지 않는다. 새로고침하면 사라진다.
- 모의 응답은 질문 내용을 실제로 이해하지 않는다. 화면과 흐름을 보기 위한 것이다.
- OpenAI · Google 모델 id는 예시값이다. 계정에서 쓸 수 있는 id로 `lib/models.ts`에서 바꿔야 한다.
